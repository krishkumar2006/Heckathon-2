apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-app-config
  namespace: todo-app
  labels:
    app: todo-chatbot
    phase: "4"
data:
  FRONTEND_URL: "http://localhost:30000"
  ENVIRONMENT: "production"
